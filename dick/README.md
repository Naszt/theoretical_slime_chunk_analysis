# 筛 dick

该目录下的所有算法都是针对筛 dick 功能的。  
这个算法的原理就是先筛 $2\times6$ 的长条，再判定剩下 $8$ 个位置。

其中 `dick.cpp` 就是实际代码，其中多线程和文件输出部分是 AI 写的。  
之所以要文件输出是因为怕程序崩了，但实践下来其实没问题（  
如果你看不懂可以看注释掉的部分代码。

`read.cpp` 就是合并 `dick.cpp` 输出的文件到 `result.txt` 里面。  
现在的这个文件是筛了 $c\in[0,14600000]$，有 $182$ 个解。
